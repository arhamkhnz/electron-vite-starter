import { defineConfig } from "oxlint";

export default defineConfig({
  // This starter intentionally defines no project-specific lint configuration.
  // Add plugins, rules, environments, and overrides based on your project's requirements and structure.
  // Documentation: https://oxc.rs/docs/guide/usage/linter
});
